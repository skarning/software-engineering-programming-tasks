public class TestingTools {
    public static boolean isDividableWith(int numerator, int denominator) {
        if (numerator % denominator == 0)
            return true;
        return false;
    }
}
